server {
  listen 80;
  server_name claimease.in www.claimease.in;
  root /var/www/claimease;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location ~* \.(js|css|png|svg|ico|webmanifest)$ {
    expires 7d;
    add_header Cache-Control "public";
  }
}
